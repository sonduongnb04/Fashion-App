const express = require('express');
const router = express.Router();

// router.post('/', (req, res) => { /* create product (admin) */ })
// router.put('/:id', (req, res) => { /* update product (admin) */ })
// router.delete('/:id', (req, res) => { /* delete product (admin) */ })

module.exports = router;


